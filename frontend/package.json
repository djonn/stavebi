{
  "name": "stave-bi-frontend",
  "version": "1.0.0",
  "author": "djonn",
  "main": "index.ts",
  "scripts": {
    "build": "rollup -c -o ../src/wwwroot/script.js",
    "watch": "rollup -c --watch -o ../src/wwwroot/script.js",
    "clean": "rm -rf ../src/wwwroot && mkdir ../src/wwwroot && touch ../src/wwwroot/.gitkeep",
    "release": "rollup -c -o out/script.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "spelling",
    "bee",
    "danish",
    "dansk"
  ],
  "devDependencies": {
    "@rollup-extras/plugin-copy": "^1.9.3",
    "@rollup/plugin-typescript": "^11.1.6",
    "rollup": "^4.12.0",
    "tslib": "^2.6.2",
    "typescript": "^5.3.3"
  }
}